# DESIGN.md — Tsubasa's Portfolio / Neon Night Shift

## 目的

制作物，スキル，経歴，プロフィールを迷わず探索できる個人ポートフォリオである．
最初の画面はネオンバーの看板として印象を残し，その直後にポートフォリオの目次を置く．
雰囲気だけで終わらせず，制作物へ最短で進める構造を優先する．

## 情報設計

```
Home
├─ Hero: ブランドと本人の入口
├─ Portfolio index: Projects / Skills / Career / About
└─ Footer: 連絡先と GitHub

Projects: 制作物一覧と画像ギャラリー
Skills: 使用技術の一覧
Career: 年表
About: 基本プロフィール
```

- Home の主目的は「Projects を見てもらうこと」である．
- 4 つのカテゴリは常に同じ順序で提示する．
- 各詳細ページの本文，制作物の説明，年表の文言は編集しない．

## デザインコンセプト

**Neon Night Shift**．暗いレンガ壁の前に掛けられたアクリルネオンサインと，整った作品目録を組み合わせる．

- ネオンは主役の見出しと状態表示にだけ使う．
- カードと本文は発光させず，薄い罫線と暗い面の差で区切る．
- 丸い要素はプロフィール画像と小さな状態灯に限定する．
- 角丸は控えめにし，ポートフォリオ一覧は直線的なグリッドにする．

## 参考から取り込む原則

getdesign.md の暗色・編集型プロダクトの設計を，個人ポートフォリオ向けに翻案する．

- 濃い単色の面を基本にし，アクセント色は少数に絞る．
- 大きな導入の後に，カード一覧で閲覧先を明確にする．
- カードは「カテゴリ，タイトル，行き先」の順に読めるようにする．
- 1px の罫線，十分な余白，軽い文字色を情報整理に使う．
- 画面幅が狭くなるほど，グリッドの列数を減らして内容を縮小しすぎない．

## カラー

| Token | Value | 用途 |
| --- | --- | --- |
| `--ink` | `#090708` | 基本背景 |
| `--surface` | `rgba(31, 20, 27, .78)` | 情報面 |
| `--paper` | `#fff7ef` | 主本文・見出し |
| `--muted` | `#d6c9c9` | 補助本文 |
| `--pink` | `#ff3ca6` | 主ネオン・強調 |
| `--pink-light` | `#ff95ce` | ネオン文字・ラベル |
| `--cyan` | `#50e8ff` | 状態表示・補助強調 |
| `--amber` | `#ffbf4c` | プロフィールサイン |
| `--line` | `rgba(255, 220, 233, .19)` | 罫線 |

ピンクを主役にする．シアンとアンバーは同じ領域で競合させず，状態表示とプロフィールに役割を分ける．

## タイポグラフィ

- 表示見出し: `Playfair Display`，日本語フォールバック `Noto Sans JP`
- 本文: `Noto Sans JP`
- ラベル，日付，ナビゲーション: `DM Mono`

| Role | サイズ | 使い方 |
| --- | --- | --- |
| Hero sign | `clamp(4.25rem, 10vw, 8.5rem)` | トップの `Portfolio` のみ |
| Section heading | `clamp(2rem, 5vw, 3.5rem)` | 一覧の見出し |
| Page heading | `clamp(3rem, 7vw, 5.4rem)` | 詳細ページの見出し |
| Body | 0.9–1rem | 説明文 |
| Mono label | 0.58–0.75rem | 補助情報 |

## コンポーネント

### ネオンサイン

- アクリル板を想起させる二重枠を使う．
- 発光は文字，外枠，状態灯に限定する．
- 反射はごく薄い斜めのハイライトで表現する．
- 読みにくくなる点滅アニメーションは使わない．

### ポートフォリオカード

- `番号 → カテゴリ → タイトル → 行き先` の読順とする．
- 1px 罫線で連結したグリッドにする．
- ホバー時は面の明るさと下線だけを変える．
- Projects だけを横長の注目カードにする．

### 詳細ページのカード

- 制作物，経歴，スキルの情報は暗い面と罫線で整理する．
- 本文の可読性を優先し，本文領域に強いネオンを置かない．
- 外部リンクはシアンのアウトラインで識別する．

## レスポンシブ

| 幅 | レイアウト |
| --- | --- |
| 721px 以上 | ヒーローはプロフィールと看板の 2 列，目次は 4 列 |
| 431–720px | ヒーローは 1 列，目次は 2 列 |
| 430px 以下 | ヒーローと目次は 1 列，カードを縦に並べる |

- モバイルのメニューはハンバーガーに切り替える．
- 操作対象は最低 44px 程度の高さを保つ．
- `prefers-reduced-motion` 利用時はすべての遷移を止める．

## アクセシビリティ

- 見出し階層を保ち，画像には内容を示す代替テキストを付ける．
- キーボードフォーカスを色と枠で明確に示す．
- 色だけで状態を表さず，`OPEN LATE` のように文字も併用する．
- 文字のコントラストを確保し，長い文章にネオンの影を重ねない．
